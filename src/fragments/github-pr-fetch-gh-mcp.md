**For GitHub PRs:**

Use the GitHub MCP tool: `mcp__github__pull_request_read` with `method: "get_diff"`

If GitHub MCP is not available, show:
```
GitHub MCP not configured!
See: https://github.com/modelcontextprotocol/servers/tree/main/src/github
```
